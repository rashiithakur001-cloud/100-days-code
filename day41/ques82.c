//Print each character of a string on a new line.
#include<stdio.h>
int main(){
char string[80];             
 printf("enter a string: ");      
  scanf("%s",&string);
 for(int i = 0 ; string[i] != '\0' ; i++){   //print each char in new line
 }
 return 0;  
}                           